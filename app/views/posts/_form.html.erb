<%= form_with(model: [@topic, post], local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sport_name %>
    <%= form.text_field :sport_name %>
  </div>

  <div class="field">
    <%= form.label :player_name %>
    <%= form.text_field :player_name %>
  </div>

  <div class="field">
    <%= form.label :jersy_no %>
    <%= form.text_field :jersy_no %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
